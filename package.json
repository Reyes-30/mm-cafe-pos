{
  "name": "mm-cafe",
  "version": "1.0.0",
  "description": "M&M Café y Más... - Sistema POS",
  "scripts": {
    "install:all": "cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npx prisma generate && npm run build",
    "build": "npm run install:all && npm run build:frontend && npm run build:backend",
    "start": "cd backend && node dist/server.js",
    "seed": "cd backend && npx tsx prisma/seed.ts",
    "migrate": "cd backend && (npx prisma migrate deploy || echo Prisma migrate bloqueado, se continua deploy)",
    "fix-migration": "cd backend && npm run fix-migration"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
